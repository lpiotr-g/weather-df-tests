definition:
  cluster_config:
    master_config:
      num_instances: 1
      machine_type_uri: "n1-standard-4"
    worker_config:
      num_instances: 2
      machine_type_uri: "n1-standard-4"
    gce_cluster_config:
      service_account: "{{ service_account }}"
      subnetwork_uri: "{{ subnetwork_uri }}"
