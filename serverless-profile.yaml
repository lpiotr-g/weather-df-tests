name: batch-resource-profile-test-1
type: dataproc.session  
definition:
  environmentConfig:
    executionConfig:
      serviceAccount: "{{ service_account }}"
      subnetworkUri: "{{ subnetwork_uri }}"
    runtimeConfig:
      version: "2.3"
      properties:
        spark.app.name: "path-file-resource-profile"
        spark.executor.instances: "2"
        spark.driver.cores: "4"